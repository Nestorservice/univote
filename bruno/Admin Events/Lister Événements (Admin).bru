meta {
  name: Lister Événements (Admin)
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/admin/events?page=1&limit=20
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  # Lister les Événements (Admin)
  Retourne tous les événements y compris les brouillons.
  
  **Params:** `page`, `limit`
}
